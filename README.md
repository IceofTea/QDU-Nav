# QDU 校园导航

> **🌐 在线访问：<https://IceofTea.github.io/QDU-Nav/>**（由 GitHub Actions 自动构建并部署到 GitHub Pages，见下方「部署」）
>
> **🏷️ 当前版本：v1.3.17**（校历原图预览版：每学年校历本地预览 + 丝滑滑动切换 + 全屏缩放查看。版本号在 `src/config/site.js` 中维护，页脚与「关于本站」面板同步展示）

面向青岛大学的校园服务聚合入口（非官方演示站），亮色清新、移动端优先。**本站同时是一套可直接复用的「学习导航站」工程模板**：品牌、文案、配色、应用注册、数据源均已模块化，参考本仓库改造为其他学校 / 其他主题的导航站成本极低（见「二次开发：移植与扩展」）。

## 功能

| 应用 | 说明 |
| --- | --- |
| 🏛️ 学校官网 | 学校主站 / 教务 / 招生 / 服务入口大全；**学院官网按学科分类**（人文社科 / 理工 / 医学 / 艺术与体育 / 合作办学）；社区与资源含**青岛大学吧**链接 |
| 📢 校园动态 | 教务处官网通知/动态，随快照定时更新 |
| 🗓️ 课程表 | **真实课表查询**（班级 / 教室 / 教师 → 周课表），支持**学期切换**与**按周次筛选**；班级列表**分页浏览**（每页 10 条 + 智能页码选页/跳转 + 「展开全部/收起」，避免千级班级卡顿）；数据来自教务处课程总表，**合并最近 7 个学期**（约 5 万条排课，教室并集 780+ 间） |
| 🧭 教室导航 | 空教室查询（结果独立页）+ 教室一周占用 + 教学楼导航指引 |
| 📅 校历 | **每学年校历原图本地预览**（2019~2020 学年起 9 期，左右按钮 / 下拉 / 拖动滑动切换，带方向滑动动画）+ **全屏查看器**（缩放 / 拖动平移 / 键盘翻页）+ PDF 原件直链（免验证码）+ 官方校历入口 + 学期时间线 |
| 🍽️ 食堂空座率 | 官方食堂名单 + 营业时段实时判定 + 真实档口/招牌菜 |
| 🪪 新生学号查询 | 官方录取查询入口与流程引导（录取系统需校内环境，不做代查） |
| 📊 数据洞察 | 基于近 7 学期 5 万条排课的统计：热门教室 / 教师 / 课程、学期趋势、周节次分布，并展示**课程性质 / 校区 / 学院开课分布**（仅在真实标注数据存在时展示，过滤「未标注」项，占比按已标注样本计算；分布随定时抓取补充） |
| 💪 体测计算器 | 按《国家学生体质健康标准》计算体测总分与等级 |
| 🍜 今天吃什么 | 随机推荐食堂真实档口，支持**按校区筛选**（浮山 / 金家岭 / 松山） |
| 🧮 生活费计数器 | 收支随手记（**批量记账**）+ **微信/支付宝/建行账单导入**（真实 CSV / xls / xlsx 自动识别、仅本地解析、自动归类、**退款冲抵 + 智能清洗中转 + 去重 + 按月份归入**）+ **生活费专业版**（近 12 月收支图表 / 同商户支出排行 / 明细筛选 / 导出 CSV 分析）+ **生活费模拟三合一** + **奖学金预设** + **分级成就墙**（20 个）与彩蛋 |
| 🎯 青大知多少 | **102 题**校园知识问答，含**错题回顾**与**本机排行榜** |
| 🎡 美食轮盘 · 🧩 教学楼速配 | 校园小游戏 |
| 📣 贴吧舆情 | 爬取**青岛大学吧**公开列表页的轻量舆情分析：热帖榜 / 关键词 / 话题分布 / 近 14 天发帖趋势（贴吧反爬较严时如实降级显示空态，随仓库内置空结构文件避免 404，附官方入口；数据由定时任务尽力抓取） |
| 🏆 贡献者墙 | 词云式展示项目贡献者，点击跳转 GitHub 主页；社区 PR 合入者在此致谢 |
| 📊 本站舆情 | 自建计数服务的**多维访问统计**：独立访客 / 累计访问 / 今日访客 / 今日访问 + 近 7 天趋势、24 小时时段、一周分布、设备 / 系统 / 来源占比、热门应用 Top（打开应用自动上报，纯自动化） |
| 🎓 校领导测试 | 35 题校园管理场景 + 多维原型比对，测出你像哪位青岛大学校领导（10 位原型附真实照片） |

## 技术栈

- **前端**：Vue 3 + Vite，静态资源部署，支持 Hash 路由分享
- **网关**：原生 Node HTTP（`server/index.mjs`，端口 8787），抓取并解析教务数据
- **Python 数据侧**：`crawler/`（仅标准库）承担**爬取 → 校验 → 测试 → 分析 → 差异**整条数据链路，与 Node 版双实现互为保障
- **数据层**：快照 `public/data/snapshot.json`（含最近 7 个学期课程总表全量排课，前端 API 请求失败时自动回退快照，保证纯静态托管可用）+ 洞察 `public/data/course_stats.json` + 贴吧舆情 `public/data/tieba_stats.json`

### 前端模块划分（`src/`）

| 目录 / 文件 | 职责 | 移植时 |
| --- | --- | --- |
| `config/site.js` | **站点唯一配置**：名称、品牌、版本、版权、来源说明、外部链接 | ✅ 改这里即可换品牌与文案 |
| `router.js` | 视图注册表（`VIEWS`）+ 底部导航（`NAV_APPS`）+ Hash 路由解析 | ✅ 新增/调整应用入口 |
| `data/` | 全部静态数据（应用注册表、校区、食堂、教室、题库等） | ✅ 换成本校数据 |
| `api/` | 网关优先 + 快照兜底的数据访问层 | 一般不动 |
| `utils/` | 公共工具（课程解析 / 格式化） | 一般不动 |
| `views/` | 各应用页面（每个 `.vue` 一个应用） | ✅ 增删应用 |
| `components/` | 可复用组件（如 `CountUp` 数字滚动） | 一般不动 |
| `styles.css` | 全局样式，`:root` CSS 变量控制主色调 | ✅ 换配色只改变量 |

### Python 数据链路（`crawler/`）

| 模块 | 职责 | 入口 |
| --- | --- | --- |
| `build_snapshot.py` | 抓取课程总表（xlsx 下载+解析）、通知、动态、校历 → 生成快照 | `python crawler/build_snapshot.py` |
| `validate.py` | 快照质量门禁（schema / 数量 / 一致性），CI 提交前强制运行 | `python crawler/validate.py` |
| `analysis.py` | 聚合课程数据 → 数据洞察页数据 | `python crawler/analysis.py` |
| `tieba.py` | 尽力抓取「青岛大学吧」列表页 → 舆情分析（热帖/关键词/话题/趋势）；失败保留旧数据 | `python crawler/tieba.py`（Node 回退 `node scripts/tieba.mjs`） |
| `diff.py` | 对比上次快照输出变更摘要，写入 CI 提交信息 | `python crawler/diff.py` |
| `fetcher.py` / `parsers.py` / `config.py` | 通用抓取 / 页面解析 / 配置 | — |
| `qdu_crawler.py` | 公开公告与校历图片抓取（工具演示） | `python crawler/qdu_crawler.py --calendar` |
| `server/parse_kcb.py` | 课程总表 xlsx 解析（纯标准库） | — |

**质量保障**：`tests/` 含 18 个单元测试（解析器 / 快照 schema / xlsx 解析 / 公开抓取 / 贴吧解析），CI 与本地 `python -m unittest discover -s tests` 均会运行；快照在任何提交前都必须通过 `validate.py`，防止学校改版导致数据静默退化。

## 数据来源与爬取原理

网站所有「实时」数据均来自**青岛大学官网公开页面**（`jwc.qdu.edu.cn` 教务处），本站不做任何数据转发/代理，仅定时抓取后静态聚合展示：

| 数据 | 来源 | 抓取内容 |
| --- | --- | --- |
| 教务通知（约 60 条） | 教务处**通知列表** `jwtz.htm` 及分页 `jwtz/N.htm`（前 4 页合并去重） | 标题 / 日期 / 原文链接 |
| 教学动态（3 条） | 教务处首页 `index.htm` | 标题 / 封面图 / 原文链接 |
| 校历（约 15 条） | 教务处校历列表 `xl.htm` | 标题 / 日期 / 原文链接 |
| 课程总表（7 学期，约 5 万条排课） | 教务处课程总表列表 `xxgk/kcap.htm` → 逐学期详情页下载 `xlsx` → `server/parse_kcb.py` 解析（纯标准库） | 课程 / 学院 / 教师 / 班级 / 周次 / 节次 / 教室 |
| 贴吧舆情 | 百度贴吧「青岛大学吧」公开列表页 `tieba.baidu.com/f?kw=青岛大学`（前 4 页约 200 帖） | 标题 / 作者 / 回复数 / 发帖时间 |

解析规则与上游页面结构一一对应（`notice_box` 列表项、`download.jsp` 附件链接等），学校改版时需要同步更新解析器。爬取频率克制（每 6 小时一次），仅在公开页面抓取公开信息。

## 数据更新（定时爬取）

网站数据不是「部署时的一次性快照」，而是**持续定时爬取**：

- GitHub Actions `refresh-snapshot` 工作流**每 6 小时**（北京时间 08:23 / 14:23 / 20:23 / 02:23）自动运行：**Python 版爬虫**（`crawler/build_snapshot.py`，失败自动回退 Node 版 `scripts/snapshot.mjs`）→ 重新抓取全部学期课程总表、通知（前 4 页约 60 条）、动态、校历 → 生成洞察数据（`analysis.py`）→ **贴吧舆情**（`tieba.py`，反爬失败时跳过并保留旧数据，不阻塞）→ **质量校验**（`validate.py` + 单元测试）→ 计算差异摘要（`diff.py`）→ 有变化时提交推送 main 分支
- 推送自动触发 `deploy` 工作流重新构建并部署 GitHub Pages，因此线上站点始终反映**最近一次抓取**的数据

### 手动更新（随时触发）

**方式一：网页一键触发（推荐）**
1. 打开仓库 **Actions** 页签
2. 选中左侧 **refresh-snapshot** 工作流
3. 点 **Run workflow** → 绿色按钮，立即在 GitHub 云端重跑抓取并自动部署

**方式二：本地命令行**
```bash
python crawler/build_snapshot.py   # Python 版（首选）
python crawler/analysis.py         # 生成数据洞察统计
python crawler/validate.py         # 质量校验（可选，CI 会自动跑）
# 或
node scripts/snapshot.mjs          # Node 版（等价）
git add -A && git commit -m "data: refresh snapshot" && git push origin main
```
推送到 main 后 `deploy` 工作流会自动构建部署，线上即更新。

> 课程总表为学校定期发布的公开文件（通常每学期更新一次）；通知、动态、校历则随官网实时变化，每 6 小时抓取足够及时。若遇官网临时维护导致抓取失败，工作流会自动跳过本次、保留上次快照，不影响线上可用性。

## 本地运行

```bash
npm install
npm run dev        # 前端开发服务器
node server/index.mjs   # 数据网关 8787（可选，前端无网关时回退快照）
```

## 部署

- 推送/定时任务提交到 `main` 分支，即由 `deploy.yml`（GitHub Actions）自动执行 `npm run build`，将产物发布到 **GitHub Pages**，全程无需手动干预
- 仓库 Pages 的 **Source** 已配置为 **GitHub Actions**（由 `actions/deploy-pages` 发布），后续版本更新无需再修改任何 Pages 设置
- `refresh-snapshot`（数据）与 `deploy`（构建）两个工作流相互独立：数据变化才触发重新部署，避免无谓构建

## 目录结构

```
QDU-Nav/
├── src/                    # Vue 前端
│   ├── config/site.js      #   ★ 站点唯一配置（品牌/版本/文案/版权，移植入口）
│   ├── router.js           #   ★ 视图注册表 + 底部导航 + Hash 路由
│   ├── main.js / App.vue   #   应用入口 / 根组件（组装 Welcome + 顶栏 + 视图 + 页脚）
│   ├── styles.css          #   全局样式（:root CSS 变量控制主色调）
│   ├── api/                #   数据访问层（网关优先 / 快照兜底 / 数据洞察）
│   ├── utils/              #   公共工具（课程班级解析 / 时间格式化）
│   ├── components/         #   可复用组件（CountUp 等）
│   ├── data/               #   静态数据（应用注册表 / 校区 / 食堂 / 教室 / 题库…）
│   └── views/              #   应用页面（一个 .vue 一个应用）
├── server/
│   ├── index.mjs           # 原生 Node 网关（8787）
│   └── parse_kcb.py        # 课程总表 xlsx 解析（仅 Python 标准库）
├── crawler/                # Python 数据侧（仅标准库）
│   ├── config.py           # 数据源 / 抓取参数 / 输出路径
│   ├── fetcher.py          # 通用抓取（超时 / 重试）
│   ├── parsers.py          # 页面结构解析（通知 / 动态 / 多页合并）
│   ├── build_snapshot.py   # 快照构建器（定时任务首选入口）
│   ├── validate.py         # 快照质量校验（CI 质量门禁）
│   ├── analysis.py         # 课程数据洞察聚合
│   ├── diff.py             # 快照差异摘要
│   └── qdu_crawler.py      # 公开公告 / 校历图片抓取（工具）
├── tests/                  # Python 单元测试（解析器 / schema / xlsx / 公开抓取）
├── scripts/
│   ├── snapshot.mjs        # Node 版快照抓取脚本（等价实现，回退用）
│   └── gen-classrooms.mjs  # 楼宇/教室数据生成（从课程总表派生）
├── public/data/snapshot.json     # 定时更新的数据快照
├── public/data/course_stats.json # 定时更新的数据洞察统计
└── .github/workflows/      # snapshot.yml 定时爬取 / deploy.yml 构建部署
```

## 二次开发：移植与扩展

本站按「**改配置 → 换数据 → 增应用**」三步即可改造成任意主题的学习导航站，视图与逻辑无需改动。

### 1. 换品牌与文案（3 分钟）

编辑 `src/config/site.js` 一个文件即可：`name` / `brand` / `tagline` / `motto` / `version` / 版权与来源说明 / Wiki 社区链接。顶栏、欢迎页、页脚、首页关于面板、数据来源声明会全部同步更新。

### 2. 换主色调（1 分钟）

编辑 `src/styles.css` 顶部的 `:root` 变量（`--primary` 主色、`--accent` 强调色、`--bg` 背景等），全站配色即换，无需改任何组件。

### 3. 换成本校数据

- **静态数据**：`src/data/` 下的校区、食堂、教室、题库等直接替换为本校内容；
- **动态数据**：本仓库的爬虫链路（`crawler/` + `scripts/snapshot.mjs`）面向「教务处公开栏目」设计——把 `crawler/config.py` 的抓取 URL 与 `crawler/parsers.py` / `server/parse_kcb.py` 的解析规则换成目标站点的页面结构即可；若暂无爬虫，也可以直接准备一份符合 `snapshot.json` schema 的静态文件放进 `public/data/`（schema 定义见 `tests/test_snapshot_schema.py`），站点即可离线工作。

### 4. 新增一个应用（3 步）

1. 在 `src/views/` 新建页面组件（可仿照任一现有应用；页面内统一通过 `emit('back')` 返回、`emit('open', appId)` 跳转其他应用）；
2. 在 `src/data/apps.js` 的 `apps` 数组追加一项 `{ id, title, desc, icon, color, group, link: '#/app/<id>' }`（首页网格与应用分类会自动出现）；
3. 在 `src/router.js` 的 `VIEWS` 注册表登记 `id → 组件`；如需出现在底部导航，再在 `NAV_APPS` 加一项；
4. （推荐）在 `src/data/searchIndex.js` 的 `EXTRA` 中为该应用补充 `keywords`（别名）与 `content`（功能点），让全局搜索能按功能直达。

完成。移除一个应用同理：删视图、删 `apps` 条目、删 `VIEWS` 登记即可。

### 5. 版本管理约定

- 版本号只维护在 `src/config/site.js` 的 `SITE.version`，页脚与「关于本站」自动展示；README 顶部的版本行请在发版时同步更新；
- 建议语义化版本：`1.x.y`（功能迭代）/ `2.x.y`（较大重构）。

## 关于与版权

- **网站开发者**：炎黄YH
- **数据版权**：站内数据抓取自青岛大学官网公开页面，版权归青岛大学及相关版权方所有；本站仅聚合展示并在界面标注来源
- **用途**：本站为学习与校园生活便利而制作，内容仅供学习交流与实用参考，**不用于任何商业目的**
- 页面脚本均为本站原创（MIT 许可），但爬取的课程数据、通知、校历等文本版权归属原发布方

## 版本历史

| 版本 | 说明 |
| --- | --- |
| **v1.3.17** | **校历原图预览版**（参考 FJNU-Nav 校历方案）。①新增**每学年校历原图本地预览**：从教务处校历详情页提取官方发布的 PDF 预览大图（1240×1753），下载至 `public/data/calendar/` 本地托管（2019~2020 学年起共 9 期 10 张，2019~2020 学年含说明页；更早学年仅附件无预览图，走官方链接），清单维护于 `src/data/calendarPreview.js`（含教务处原文与 PDF 直链，路径拼 `BASE_URL` 兼容子路径部署）；②**切换交互三件套**：左右圆形按钮（边界自动禁用）/ 学年下拉 / **触摸与鼠标拖动滑动**翻页；③**丝滑滑动动画**：页面内单图用 Vue `<Transition>` 方向性滑动（新学年从右滑入 / 旧学年从左滑入，±45% 位移 + 渐隐，`cubic-bezier(.22,.9,.3,1)`），全屏模态用 row-reverse track 整体平移（跟手阶段 `transition:none` 1:1 拖动、松手恢复 .42s 缓动，边界橡皮筋阻尼 0.35），替代生硬的弹跳切换；④**全屏查看器**（teleport 模态）：缩放 ±/1:1（最大 400%）+ 放大后拖动平移 + 键盘（←/→ 翻页、+/− 缩放、Esc 关闭）+ 相邻 slide 灰缩纵深感；⑤来源栏新增 **PDF 原件直链**（`__local` 直链无需附件验证码），保留官方校历列表 / 学期时间线 / 提示面板。验证：构建通过、CDP 校历专项 15/15（默认学年定位/图片加载/按钮方向动画/边界禁用/下拉切换/模态 9 slide/track 平移与缓动/鼠标拖拽跟手翻页/缩放层/键盘翻页/Esc 关闭/PDF 直链/列表保留/零 JS 错误）+ 全站冒烟 19 页零错误 + Python 单测 18/18 全绿。 |
| **v1.3.16** | **全局搜索索引版**（参考 FJNU-Nav 搜索方案）。①新增 `src/data/searchIndex.js` 三层检索索引：每个应用 = `apps.js` 基础字段 + `EXTRA` 手工维护的 `keywords`（别名/同义词，如「课表」「空教室」「记账」「奖学金」）与 `content`（应用内核心功能点短语），全部 18 个应用均已登记；②`searchApps()` 匹配算法：多关键词空格拆分 AND 语义（任一词零分即淘汰）、加权打分 标题3 > 别名全等2.5/包含2 > 简介1.5 > 功能点1，按总分降序并收集命中标签（≤3 个）；③首页搜索接入：结果卡片显示「匹配：xxx · yyy」主色胶囊徽章、空态提示示例关键词、placeholder 更新为「搜索应用或功能」；④分类页搜索同步接入索引（此前仅匹配标题+描述）；⑤新增应用登记流程补充第 4 步（EXTRA 补索引）。验证：构建通过、CDP 专项 14/14（功能词命中/徽章展示/content 层命中/多词 AND 与无交集空态/权重排序/大小写 uv·PV/点击跳转/分类页接入/清空恢复）+ 冒烟零错误全绿。 |
| **v1.3.15** | **计数服务降耗 + 点赞同步修复版**。①counter（Deno+Node）降耗：`/api/hit` 改轻量响应（仅 uv/pv/today）、新增轻量 `/api/likes`（只返回点赞数组）、`byDay` 仅保留近 120 天清理（KV 对象不再无限膨胀）、`getStats` 兼容旧 KV 数据；②前端降耗：`reportApp` 改为会话内每应用限报 1 次、`VisitStats` 会话内只报 1 次 hit（其余读 `/api/stats`）、`fetchLikes` 改调 `/api/likes` + 60s 会话缓存，请求量约 -70%；③本站舆情新增「关于本站统计与点赞」说明面板（纯静态逻辑 / 免费额度与节流说明）；④**点赞同步修复**：服务端持久化点赞（跨浏览器可见），轻量 `/api/likes` 读取。验证：Node counter 8/8（轻量 hit / likes / 重启持久化 / byDay 清理）、CDP 4/4、回归全绿。 |
| **v1.3.14** | **公告与首页优化版**。①首页顶部新增**公告**：静态文件 `public/data/announcements.json` 可随推送更新，hero 下展示（可展开/收起），首条为**部署逻辑与安全说明**（纯静态无后端、不窃取数据、跳转均为学校官方链接、提醒勿点陌生链接）；②**点赞交互调整**：首页应用卡片改为**只读点赞数**（不遮挡卡片文本），点赞/取消按钮移至「应用分类」各卡片；③首页「应用分类」分区并入「公开应用」（查看全部分类按钮右对齐，「按学习/生活/游戏分组浏览全部 N 个应用」提示保留在卡片下方）；④**顶部栏移动端适配**：logo / 站名 / 按钮 `white-space:nowrap` 防换行，窄屏缩小字号与间距。验证：构建通过、CDP（公告显示/分类卡片点赞/顶部栏无换行）全绿、回归全绿。 |
| **v1.3.13** | **点赞系统版**。①计数服务（Deno + KV）新增 `GET /api/like?app=&vid=&on=`：每人每应用限赞 1 次、可取消、可再点回（KV `lk` 幂等去重），`/api/stats` 返回 `likes` 点赞榜；②首页 hero 区新增「👍 给本站点赞」；③首页应用网格每张卡片显示点赞数并可点击点赞/取消；④应用内（App 顶栏下方）提供点赞条；⑤本站舆情新增「🏆 最受欢迎（点赞榜）」；⑥前端 `src/utils/like.js`（`fetchLikes`/`likedByMe`/`toggleLike`，localStorage `qdu_liked` 记忆已赞，counter 不可达时本地降级）。验证：Node counter 点赞单测 7/7、CDP 前端 8/8、回归全绿。 |
| **v1.3.12** | **收支分析增强版**。①结余趋势折线支持**负半轴**（亏损月份在 Y 轴负半轴，正负分区背景）；②专业版新增**收入分类构成**与**同商户收入排行**（条形/圆饼，点击筛选明细）；③主视图与专业版明细新增**全部 / 支出 / 收入 大分类**筛选（含收入分类 chips）；④导出升级为 **Excel(.xlsx)**：汇总 sheet 支出分类/收入分类、商户支出/商户收入并列，明细 sheet 带自动筛选 + 冻结首行 + 列宽 + 收入绿/支出红会计样式；⑤建行导入增强：**平台收益提现（抖音/快手/平台/签到奖励）计入收入**，账户自身中转（余额宝/零钱/充值/投资）仍过滤，收入分类更准。验证：构建通过、CDP 12 项 + 建行导入 + 回归全绿。 |
| **v1.3.11** | **饼图 / 折线展示体系优化**。折线图重构（ResizeObserver 动态 viewBox、固定显示高度、文字与数据点不变形、水平辅助线跟随、限宽居中）；圆饼图重构（SVG 扇区、hover 饼块显示详情、扇区放大、图例紧凑饱满、中心合计、数据字段映射修复）。验证：构建通过、CDP 图表专项 + 回归全绿。 |
| **v1.3.10** | **工具与分类优化**。新生学号查询校验修正；教学楼速配卡背加载失败回退；应用分类分组色抽取 + 搜索增强；apps 注册表清理死字段；教室导航楼层教室胶囊化防溢出。 |
| **v1.3.9** | **数据 / 舆情统计页优化**。数据洞察学期趋势复用组件、KPI 口径修复；贴吧舆情话题分布组件化、14 天趋势柱状 hover 显示帖数（可切折线）；本站舆情恢复经典柱状视觉（金色序号）+ 一眼看懂洞察 + 柱状/折线/圆饼三态切换 + 热门应用圆饼全量。 |
| **v1.3.8** | **生活费专业版图表优化**。专业版「结余趋势 + 收支日历」宽屏两栏协调布局；区间收支柱状/折线切换；结余趋势折线正规化（Y 轴刻度 / 网格 / 数据点）；支出构成 / 同商户排行 / 本月构成支持圆饼。 |
| **v1.3.7** | **体验优化 + 贡献者墙精简版**。①专业版电脑端样式修复（收支日历/折线/月年网格限宽居中，手机不受影响）；②收支日历周视图改 3×2 卡片（近 6 周）；③结余趋势折线图支持悬浮/点击查看节点数据；④区间预设去「全部」改 近2年/近3年；⑤明细分页支持输入跳转页码；⑥生活费模拟新增「生活参考」tab（青岛物价速览 / 学期消费节奏 / 生活费小贴士）；⑦贡献者墙精简（v1.3.5–v1.3.7 合并一条，统一指向 README）。验证：构建通过、CDP 10 项 + 回归 v18(23)+v19(12) 全绿。 |
| **v1.3.6** | **生活费专业版 + 建行账单 + 退款冲抵 + 成就升级**。①**建行账单导入**：新增 `src/utils/xlsReader.js` 手写 OLE2/BIFF8 解析器，支持老式 .xls（Excel 97-2003）；识别建行「摘要/交易金额/交易日期」格式，金额正负映射收支，自动跳过提现/充值/零钱/余额宝/投资理财等账户中转。②**退款智能冲抵**：微信退款自动与同商户同金额原支出配对并标记「已退款」，不再虚增收支。③**商户聚合**：所有导入记录提取商户名，专业版「同商户支出排行」集中展示每个单位/店铺的收支。④**生活费专业版**（`BudgetPro.vue`）：KPI 四卡、近 12 月收支双柱对比、支出分类构成、同商户支出排行（图表可点击筛选明细）、明细排序与筛选、导出 CSV 分析文件（Excel 可直接打开）。⑤主视图明细支持排序（日期/金额/分类）+ 分类筛选；⑥成就墙折叠展示（默认 4 个，已解锁优先）+ 分级（🥉初阶/🥈进阶/🥇大师）+ 进度条。同日补更：收支日历（日/周/月/年四视图，当日盈亏着色、点击筛选明细）、区间收支柱状/折线切换与结余趋势折线、支出构成/商户排行/本月构成支持圆饼图、主视图与专业版明细分页、金额升降序、成就墙可收起。验证：Node 解析单测 4/4、Python 单测 18/18、CDP 16 项 + 回归 v18(23)+v19(12) 全绿。 |
| **v1.3.5** | **账单导入对齐 + 生活费模拟升级 + 隐藏彩蛋版**。①**账单导入功能完全可用**：新增 `src/utils/billImport.js` 统一解析器，对齐真实微信 / 支付宝账单——支付宝 GBK 编码 CSV、微信竖线分隔 / Excel(xlsx) 均自动识别，编码（UTF-8/GBK）与分隔符自动探测、RFC4180 引号解析、Excel serial 日期自动转换；按「收/支」记账，自动跳过「不计收支 / 中性交易 / 关闭 / 失败」记录；支付宝按「交易分类」映射归类、微信按商品名 / 交易对方关键词猜测；导入结果标注识别到的品牌、导入笔数、金额与跳过笔数。实测两份真实账单金额与微信支付宝官方统计完全一致（微信 18 笔 / 支出 356.98 / 收入 889.11；支付宝 44 笔 / 支出 711.64 / 收入 9.72）。②**收支分类扩充**：支出新增美容美发 / 数码家电 / 运动户外 / 网络虚拟 / 房屋住宿 / 转账支出（共 18 类），收入新增退款 / 理财收益 / 转账收入（共 9 类），覆盖微信支付宝全部交易类别。③**导入面板隐私说明**：注明本站为纯静态网页、账单仅在本机浏览器解析、不上传任何服务器、记录只存 localStorage。④**生活费模拟升级三合一**（估算 / 预算分配器 / 账单校准）：分配器输入月预算后拖动各项占比实时算金额与结余、一键套用省/标准/舒适档位、保存方案到本机；账单校准读取本机记账记录自动算出真实月均支出与构成、与参考区间对比给结论，并可将真实占比一键抄进分配器。⑤**隐藏彩蛋**（`Budget.vue`，不影响正常情绪反馈，每项 24h 内只触发一次）：金额彩蛋（生活费 >10000 且当月盈利 >10 万 / 亏损 10 万~100 万 / 超 100 万 / 聚餐 >100 / 闯祸费分档 / 其它支出 9876547210.33 / 红包奖金 500000 / 404）；隐藏皮肤「赛博账本」（连续点收入或支出 5 次或长按 3 秒）；像素雨（快速点生活费模拟按钮 3 次）；节日结算按钮变色（春节 ±3 天 / 愚人节 / 校庆 5-14）；成就「奶茶品鉴师」（奶茶消费累计 20 笔）；彩蛋文本「1+1=」连击 10 次。⑥**默哀模式**：纯静态后门——部署时向 `public/` 上传一个 `mourning` 文件即触发全站灰白 + 顶部哀思条；**每年 12 月 13 日南京大屠杀死难者国家公祭日自动变灰白**（跨日自动恢复），两种来源文案不同，不依赖后端、可随时手动开关。⑦新增 `scripts/test_billimport.mjs` 解析器单测（虚构样本，不含真实数据）；`example/` 目录（含本机账单与参考 apk）加入 `.gitignore` 防误提交。**同日补更**：⑧**批量记账**（一次记入 N 条相同记录，备注自动带序号）；⑨文本彩蛋去除 24h 冷却、月结余彩蛋改为结余卡片常驻；⑩**20 个隐藏成就墙**（奶茶品鉴师 / 干饭人 / 社交达人 / 卷王 / 记账达人 / 三个月全勤 / 奖学金收割机 / 睡后收入 等，解锁前 🔒？？？ 灰显）；⑪像素雨触发点移到「本月结余」卡片、1+1= 彩蛋改为按回车连击；⑫**导入智能清洗 + 去重 + 月份归入**：可开关跳过转账/红包/收款大额中转（≥1000 元，防过账污染收支）、重复导入自动去重不再翻倍、记录按各自日期归入对应月份并自动切到最新月份。验证：Node 解析单测 3/3、Python 单测 18/18、构建通过、CDP 导入/彩蛋/成就验证 + 回归 v18(23)+v19(12) 全绿。 |
| **v1.3.4** | **独立访客统计修正版**。修复校园网场景 UV 严重低估：原按「出口公网 IP + UA」指纹去重，校园 NAT 下全校共享同几组出口 IP 且手机 UA 雷同，大量真实访客被算成同一人（PV 正常累加、UV 几乎不动）。改为**前端匿名访客 ID（`localStorage['qdu_nav_vid']`，`crypto.randomUUID()` 生成）优先去重，缺失时回退「IP+UA」指纹**：新增 `src/utils/visitor.js`（`visitorId()` 供 `VisitStats.vue` 首页统计与 `router.js` 应用上报共用，均带 `?vid=`），`counter/server.ts`（生产 Deno）与 `counter/server.mjs`（Node 联调）指纹逻辑同步为 vid 优先/IP+UA 兜底。已验证：不同 vid → UV 各 +1、同 vid 多次 → UV 不涨、无 vid → 走 IP+UA 兜底去重。不清除既有 UV/PV 历史数据（旧 KV 键保留），仅新访客按 vid 计入。说明：清浏览器数据/无痕/换设备会被计为新访客（与行业 cookie 统计一致）；vid 为纯匿名随机串不含个人信息。验证：Python 单测 18/18、构建通过、回归 v12–v19 125 项 + 深色 12 项 + 校领导端到端 10 项全绿。另：`counter/server.ts` 与 `server.mjs` 中保留的 seed 校准（150/260）是**早期误删本地数据后手动恢复的痕迹**——只补顶层 uv/pv、未同步 byDay/byRef/byDevice 等维度明细，故**累计 UV/PV 会大于各维度加总**（如 PV 500+ 但来源/时段/设备分布加总不足 500，已知口径差异，代码注释已如实说明）。 |
| **v1.3.3** | **代码质量加固版**。P0 真实 Bug：①`server/index.mjs` 通知详情标题 `split('-')` 截断含连字符标题（如「2026-2027学年…」→「2026」）改为剥离尾部站名后缀；②v-html XSS 过滤补强（`on\w+` 事件属性兼容双引号/单引号/无引号/HTML 实体四种写法）；③清理死代码（Timetable `meta` ref、StudentId `form.name`、snapshot.mjs `parseHomeNotices`）；④`validate.py` 学期格式校验（TERM_RE 接上）+ `int()` 异常保护 + `tables[0]` 空防护；⑤`parse_kcb.py` 移除硬编码开发机路径。P1 一致性：⑥`server` 网关 `clsSplit` 与前端一致（展开「[01-04]班」范围）；⑦`BuildingMatch` 手机端挑战难度降列（3 列）；⑧`api/index.js` 注释更正（`/notice` 纯静态下如实降级）；⑨`Canteen` 营业时间魔法数字加注释。P2 可维护性：⑩**公共组件提取** `BarRow` / `KpiCard` / `InsightPanel`（数据洞察/贴吧舆情/本站舆情/生活费复用，CourseStats 内联样式 95→少）；⑪`scripts/tieba.mjs` 重写对齐 Python 手机版方案（mo/q/threadlist + 移动 UA + tl_shadow 结构，Node 回退不再易 403）；⑫`utcnow()` 三处重复抽 `crawler/util.py`；⑬counter 校准标记独立 `.seed3` 文件（不再污染 data.json）+ UV 去重 Map 定期清理防内存增长；⑭`server` cache 超上限清理过期项、`/api/notices?all` 的 cached 语义修正、GET 方法限制、MIME 补 `.txt`；⑮`FoodWheel` 轮盘动画时长抽 `SPIN_MS` 常量、`QuizGame` 每题分数抽 `PER_QUESTION` 常量、`Budget` 记录 id 抽 `newId()`。验证：Python 单测 18/18、构建通过、tieba.mjs 手机版解析与 normDate 单测对齐、回归 v12–v19 125 项 + 深色 12 项 + 校领导端到端 10 项全绿。 |
| **v1.3.2** | **深色模式修复版**。①**深色模式全面修复**：顶栏与底部导航背景由写死白色改为 `--header-bg`/`--nav-bg` 变量、**「关于本站」卡片**修复 `--card-bg` 未定义变量（此前深色下仍白底）、课程表/分类/体测等按钮（`.tab`/`.chip`/`.seg-btn`/`.year-tab`/`.mini-tile`）与骨架屏、悬停高亮、提示条全部改为语义化变量；②**Bug 修复**：`Canteen.vue` 三处无效 CSS `var(--card)3e0`（修复为 `var(--soft-yellow)`）、8 处 `var(--muted)` 未定义变量统一为 `var(--text-sub)`、课程表分页省略号与弹窗关闭按钮颜色、`termTimetable.js` Web Worker 并发请求消息错配（加请求 id）、生活费计数器编辑状态下按回车误调 `add()` 新增而非 `save()` 保存、`whyText` 用户向量与原型维度标尺不一致；③**维护**：删除根目录冗余 `leader/`（正源为 `public/leader/`）、校领导照片路径拼 `BASE_URL` 兼容子路径部署、`tieba.py` 的 `analyze` 支持注入日期（单测不再随日期失效）、清理死代码、题数文档勘误（校领导测试实际 35 题，非 30）。验证：Python 单测 18/18、构建通过、回归 v12–v19 共 125 项全绿、深色专项回归 12 项全绿、校领导测试端到端（35 题完整作答 → 照片加载成功 → 维度比对正常）。 |
| **v1.3.0** | **舆情统计版**。①计数服务升级**多维自动采集**（`counter/server.ts`）：按日期 / 小时 / 星期 / 设备 / 系统 / 来源 / 应用自动累计；②新增应用**「本站舆情」**（首页网格可进）：独立访客 / 累计访问 / 今日访客 / 今日访问 四卡 + 近 7 天趋势柱状图 + 24 小时时段分布 + 一周分布 + 设备 / 系统 / 来源占比 + 热门应用 Top；③**纯自动化**：打开任意应用自动上报（`openApp` 节流上报 appId），无需手动操作；④首页统计卡片手机端优化——「本站累计 · 自建独立计数」标注移到数字下方单独一行。 |
| **v1.2.10** | **功能增强版**。①**生活费计数器**：记录支持**编辑 / 删除**（点明细行 ✎ 载入表单改错后保存）；**生活费作为固定收入**参与结余判断——顶部显示「生活费 ¥X · 已用 Y%（剩 ¥Z）」进度条，结余文案按生活费逻辑提示；②新增**生活费模拟页**（计数器内「🧮 生活费模拟」按钮进入）：**简单版**按伙食 / 社交 / 购物三档粗估月生活费区间，**精细版**按家庭月收入智能推荐区间 + 各项预算明细 + 省钱技巧 + 兼职建议；③首页 **QDU-Nav Logo 改为文字炫彩渐变**（彩虹渐变文字 + 白底卡片，参考福师大风格）；④首页 **Wiki 卡片文案实事求是**（校园生活手册，不再称学习资料社区）；⑤**贡献者墙**：贡献说明小字防溢出（截断），手机端不再出界叠字，并补记 v1.2.1–v1.2.10 社区贡献记录；⑥**课程表手机端紧凑网格**：隐藏节次时间列、7 天均分一屏，无需左右拖拽，点卡片看详情。 |
| **v1.2.9** | **统计功能版**。新增首页**访问统计卡片**（「关于本站」面板内、贡献者墙按钮下方）：展示**独立访客 UV** 与**累计访问 PV**，数据来自**自建独立计数服务**（`counter/server.ts`，Deno Deploy 新平台 + KV 持久化，生产地址 `https://qdu-nav.iceoftea.deno.net`，UV 去重由前端 localStorage + `isNewUv` 参数完成）——与 QDU-Wiki 的 Vercount 统计**完全独立**（实测 Vercount JS 不实现 `page_uv`、site_uv 按域名共享，同域两站无法独立，故自建）。部署要点：console.deno.com 新建 app（App Directory 选仓库根、Runtime=Dynamic、Entrypoint=`counter/server.ts`、Build 命令 `echo skip`、创建并附加 KV 数据库）；服务不可用时卡片降级显示「—」。 |
| **v1.2.8** | **课表逻辑优化版**。①`clsSplit` 展开「[01-04]班」范围：班级列表只出现单班（如 25软件01班…04班），不再与「25软件[01-04]班」重复并存，合班课自动落到每个单班课表；②**分段授课合并**：同一课程同一节次的多个教师（如大学英语读写译 VI 的 4~12 位分段老师）合并为一条，教师名全列；③列表视图**合班备注**：「📌 合班 25软件[01-04]班…」标注该课面向哪些班（详情弹窗亦有班级行）；④确认 21 软工创新班可在 2023–2024 学年学期正常检索（此前为操作学期/时序问题）。 |
| **v1.2.7** | **课表体验版**。①**周课表补显示任课教师**（班级课表每门课显示「教师 · 教室」，数据来自课程总表 `t` 列）；②新增**「列表视图」**：按星期分组、每门课一行（节次 / 课程 / 教师 / 教室），手机端不再挤在窄网格里；③**点击课程弹详情**：教师 / 教室 / 班级 / 时间 / 周次 / 校区 / 类别 / 学分；④「周视图 / 列表视图」一键切换。 |
| **v1.2.6** | **性能优化版**。①`termRows` 改 **`shallowRef`**：避免 Vue 把 5900+ 元素数组深度代理，遍历排课提速（此前每次属性访问走 Proxy）；②`curRows` 直接返回当前学期数据，省去每学期重复 `filter`；③**Web Worker 后台解析 JSON**：2MB 排课数据解析移到后台线程，低端机/弱网下主线程不再被 parse 阻塞，加载过程中可正常点击返回/导航。CPU 4x 模拟手机实测：冷启动约 0.6s、搜索 0.3s、切换 tab 正常（教师/教室 22 项）。 |
| **v1.2.5** | **性能优化版**。①**课程表卡顿彻底修复**：`countOf` 对每个班级全量遍历排课（O(n²)，1470 班级 × 5900 行 ≈ 870 万次 filter，手机主线程卡死）改为**一次性预计算计数表**（`counts` Map，O(n)）——打开约 **277ms**（原 8.5s，快 30 倍）、搜索/筛选即时响应（264ms）；②**智能分页**：页码不再罗列堆砌，改为「« ‹ 1 2 … 末几页 › »」折叠样式 + 首页/末页 + **输入数字跳转**，手机端清爽且 DOM 大幅减少；③解决「0 条排课 / 无法退出」的主线程卡死问题。 |
| **v1.2.4** | **体验优化版**。①**课程表加载提速（真正解决）**：`staticCourseTable` 改用 2KB 的 `timetable_meta.json`、`Timetable` 移除 `apiFetch('/courseTable')` 阻塞——此前网关不可达时会回退全量加载 15MB 快照，现实测打开约 **217ms**；②**官方新媒体链接修正**：微博 `weibo.com/u/5726029829`、抖音 `douyin.com/user/…`、微信公众号 `mp.weixin.qq.com/s/…`，并新增 **B 站官方账号**（space.bilibili.com/441692154）；③**邮箱助手美化**：卡片式渐变 + 图标头 + 俏皮提示「校长邮箱 xiaozhang@qdu.edu.cn」。 |
| **v1.2.3** | **体验优化版**。①**课程表加载提速**：按学期拆分排课数据（`timetable_meta.json` + `terms/*.json`），默认只加载当前学期（约 2MB 而非 15MB 全量快照），切换学期按需懒加载并缓存，解决首屏慢 / 手机卡死；②**校园邮箱**：官网「信息化与服务」组新增邮箱入口（mail.qdu.edu.cn），官网页内嵌**「邮箱助手」**——输入学号一键生成 `学号@qdu.edu.cn` 并复制，附开通指南；③**官网改造**：青大 Wiki 收敛为网站入口、「信息化与服务」组上移并**重点突出「网上办事大厅」**、新增**「官方新媒体」组**（官方微博 / 微信视频号 / 抖音，账号 qddx）；④**贴吧舆情趋势交互**：悬停/点击柱子显示当日发帖数，窄屏支持横向滑动；⑤**手机端修复**：体测页「分:秒」输入框右侧溢出、首页 Wiki 卡片按钮竖排；⑥**应用顺序与底部导航**调整为常用顺序（首页 / 动态 / 官网 / 生活费 / 体测 / 教室 / 校历）。 |
| **v1.2.2** | **界面增强版**。①**课程表班级列表每页 10 条**（原 50 条，短列表更清爽）+ 「展开全部/收起」与页码选页不变；②**贴吧舆情界面图表化丰富**（参考数据洞察）：新增 KPI 概览卡（抓取帖数 / 最热帖回复 / 话题覆盖 / 近 14 天发帖与日均）、自动生成文字洞察、「热帖榜」改为回复数条形图 + 作者/日期副标题、关键词改**标签云**（字号随热度放大）、话题分布显示占比百分比、14 天趋势柱状图标注日均，把抓到的数据全部可视化用起来。 |
| **v1.2.1** | **维护增强版**。①修复 **GitHub Actions 部署失败**（`tests/test_tieba.py` 解析样本仍为桌面版 HTML，与贴吧手机版解析不一致导致单测失败 → 更新为手机版结构样本，单测 18/18 恢复）；②**数据洞察附带列数据恢复**：重新抓取生成快照，课程性质 / 校区 / 学院开课分布恢复真实数据（未标注仅剩 2 条），「数据洞察」不再显示待抓取提示；③数据洞察**界面增强**：新增 KPI 概览卡（最忙校区 / 最忙星期 / 开课学院）、自动生成文字洞察、「每天节次分布」面板；④课程表**周课表单元格显示校区**，跨校区上课更直观。 |
| **v1.2.0** | **功能迭代版**。①新增**贡献者墙**：词云式展示项目贡献者（IceofTea / Huanuyn / qdu-nav-bot），点击跳转 GitHub 主页，首页应用网格与「关于本站」面板双入口；②**贴吧舆情**：`tieba.py` 反爬增强（随机 UA 池 / 随机延迟 / 指数退避重试），并随仓库提交 `tieba_stats.json` 空结构，消除部署后的 HTTP 404（抓取失败时如实显示空态而非报错）；③课程表班级列表**分页**：默认每页 50 条 + 页码选页 + 「展开全部/收起」，解决 1470+ 班级全量渲染卡顿；④数据洞察**分布科学化**：课程性质 / 校区 / 学院分布仅在存在真实标注数据时展示，过滤「未标注」项、占比按已标注样本计算（消除 100% 未标注的无意义展示）；⑤**奖学金金额校准**：优秀奖学金 1000/600/400、国家助学金三档 2300/3300/4300（青大学字〔2021〕27号），并增补竞赛奖学金（挑战杯·国特 30000 / A类·国一 5000）与博学奖学金（发明专利 5000）预设。 |
| **v1.1.1** | **社区修复版**（合入社区贡献 PR #1，致谢 huanuyn）。①修复本地 `npm run server` 网关课程接口报错：默认课程总表解析脚本路径指向不存在的临时文件，改为仓库内 `server/parse_kcb.py`，并按平台选择 `python`/`python3`（Windows 用 `python`）；②修复美食轮盘**第二次起指针停靠位置与抽取结果不一致**（把当前角度归一化后补足到目标扇区中心）；③课程快照保留 `kind/campus` 附带列——与 v1.1.0 已实现的数据链路修复一致，维持更完整实现（含 `col/cat/credit/weeks` 与学院分布），未重复合入。 |
| **v1.1.0** | **功能迭代版**。①数据链路修复：快照生成保留 `col/campus/kind/credit` 等附带列，「数据洞察」新增**课程性质 / 校区 / 学院开课分布**与解读文案（旧快照为「未标注」，下次定时抓取后自动补充真实分布）；②课程表班级列表**全量展示**（移除 40 条截断）；③生活费计数器增强：类别扩充、**微信/支付宝账单 CSV 导入**（自动归类）、**奖学金/助学金预设一键带出**、结余情绪反馈分档 + 支出月对比；④青大知多少题库扩至 **102 题**、答题选项徽章化、成绩页**错题回顾**与**本机排行榜**；⑤今天吃什么支持**校区筛选**；⑥新增**贴吧舆情**：`tieba.py`/`tieba.mjs` 尽力抓取「青岛大学吧」列表页产出热帖榜 / 关键词 / 话题分布 / 近 14 天趋势（反爬失败时如实降级并保留旧数据），**替换原本地演示版评论区**；⑦学校官网：**学院官网按学科分类**（人文社科 / 理工 / 医学 / 艺术与体育 / 合作办学），社区与资源新增**青岛大学吧**入口。 |
| **v1.0.0** | **初始模板版**。完成前端工程化重构：站点配置（`config/site.js`）、路由注册表（`router.js`）、公共工具（`utils/`）模块化拆分，消除分散文案与重复工具函数；全文件补齐模块注释；新增版本号标注（页脚 / 关于面板 / README）与「二次开发：移植与扩展」指南。此前为无版本号的功能迭代。 |

## 免责声明

非青岛大学官方服务。数据来自学校公开渠道并定时抓取，仅供学习交流，请以学校官方最新通知为准；任何接入官方系统的自动化行为请遵守校规校纪与法律法规。