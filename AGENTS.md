# AGENTS.md — 本站维护须知（每次会话必读）

> 本文件是 AI 助手与本仓库协作的「维护手册」：先读本项目须知，再读下方「维护清单」与「更新日志」，
> 确保每次改动都遵循既有架构与发布流程。更新日志的机制与 `QDU-Wiki/prompt/AGENT-GUIDE.md` 一脉相承：
> **每次发布都把「改了什么 / 为什么改 / 验证结果」追加到日志**，方便后续会话快速接续上下文。

## 一、项目是什么

**QDU-Nav（青大导航）**：面向青岛大学的纯静态校园导航单页应用。

- 技术栈：Vite + Vue 3（Composition API）+ 无后端，托管于 GitHub Pages。
- 数据策略：**「优先抓实时、回退用快照」**——有 server 网关时请求实时接口，纯静态托管下读取 `public/data/` 下的静态快照。
- 定时数据：GitHub Actions `snapshot.yml` 每 6 小时抓取教务处公开数据并自动提交；前端「数据洞察 / 贴吧舆情」等依赖该产物。

## 二、架构速览（改动前先确认位置）

| 模块 | 路径 | 说明 |
| --- | --- | --- |
| 应用注册 | `src/data/apps.js` | 首页应用网格的唯一来源 |
| 路由注册 | `src/router.js` | 应用 id → 视图组件的映射（新增页面必须双登记） |
| 站点配置 | `src/config/site.js` | 站点名 / 副标题 / 版本号（**版本号唯一维护点**） |
| 视图 | `src/views/*.vue` | 各应用页面 |
| 静态数据 | `src/data/*.js` | 校历、食堂、学院官网、题库、配置等前端内置数据 |
| 数据抓取（Python 首选） | `crawler/` | `fetcher.py` 抓取 / `parsers.py` 解析 / `build_snapshot.py` 快照 / `analysis.py` 洞察 / `tieba.py` 贴吧舆情 / `validate.py` 校验 / `diff.py` 差异摘要 |
| 数据抓取（Node 回退） | `scripts/snapshot.mjs`、`scripts/tieba.mjs` | 与 Python 版输出格式一致 |
| 静态产物 | `public/data/*.json` | `snapshot.json`、`course_stats.json`、`tieba_stats.json`（由 CI 提交） |
| 定时任务 | `.github/workflows/snapshot.yml` | 每 6h 抓取并自动提交；`deploy.yml` 构建部署 |

## 三、数据链路（新增数据源时照此办理）

1. 在 `crawler/` 新增抓取/解析脚本（标准库优先，Python 首选；保持 `scripts/` 有 Node 回退）。
2. 在 `.github/workflows/snapshot.yml` 的 crawl job 中加入步骤，并在 `git add` 中登记产物文件。
3. 前端在 `src/views/` 下新建视图，在 `apps.js` + `router.js` 双登记。
4. 抓取应「尽力而为」：失败不覆盖上一次成功数据、不阻塞整体提交（参见 `crawler/tieba.py` 的处理方式）。
5. 静态数据文件必须能被 404 优雅降级（前端做好空态），因为首次部署前产物文件可能不存在。

## 四、维护清单（每次改动必须按序执行）

1. **改完跑 Python 单测**：`python -m unittest discover -s tests`（解析器/构建器改动必须补单测）。
2. **前端构建**：`npm run build`，确认无报错、JS 体积合理。
3. **回归**：用 `node server/index.mjs` 起服务（端口 8787），跑 CDP 回归脚本 `regress_v*.mjs`（基线见 `C:\Users\13111\AppData\Local\Temp\opencode\qdunav\`），确认全绿。
4. **更新 README**：功能表 / 数据来源 / 版权说明如有变化需同步；版本历史追加一行。
5. **更新版本号**：按 `x.y.z`——小更新（bug 修复 / 文案 / 维护性改动）只加 z（如 1.1.1→1.1.2）；功能迭代 / 新功能等大更新加 y 并归零 z（如 1.1.x→1.2.0）。版本号只在 `src/config/site.js` 维护，README 头部版本行同步。
6. **追加本文件「更新日志」**：写清楚改了什么、为什么、验证结果。
7. **提交并推送**：commit message 用 `feat:` / `fix:` / `data:` 前缀；push 前先 `git fetch` 处理定时任务的自动提交（需要时 rebase）。

## 五、红线

- 不引入前端注释（除非必要）；不新增未经确认的依赖库。
- 不编造数据：抓不到就如实降级（空态 + 提示），绝不捏造课程/食堂/舆情数据。
- 版权与隐私：贴吧帖子等第三方内容只做轻量聚合，页面需注明来源。
- 版本号只在 `site.js` 维护，不要在页面模板里硬编码。

---

## 六、更新日志

| 日期 | 版本 | 内容 |
| --- | --- | --- |
| 2026-08-19 | v1.3.5 | **账单导入对齐 + 生活费模拟升级 + 隐藏彩蛋版**。①**账单导入功能完全可用**：新增 `src/utils/billImport.js` 统一解析器（纯前端、无第三方依赖），对齐真实微信 / 支付宝账单——支付宝 GBK 编码 CSV、微信竖线分隔 CSV / Excel(xlsx) 均自动识别；编码（UTF-8 BOM / UTF-8 / GBK）与分隔符（`,` `\|` `\t` `;`）自动探测、RFC4180 引号解析、xlsx zip+XML 手写解析（DecompressionStream deflate-raw、Excel serial 日期→yyyy-mm-dd 转换）；按「收/支」记账，自动跳过「不计收支 / 中性交易 / 关闭 / 失败」记录；支付宝按「交易分类」列映射归类（餐饮→food、日用→daily、交通/爱车→transport 等），微信按商品名 + 交易对方关键词猜测，扫码/转账/退款等特殊类型单独处理；导入结果标注识别到的品牌（微信/支付宝）、来源类型、导入笔数、金额合计与跳过笔数。**实测两份真实账单金额与微信支付宝官方统计完全一致**（微信 xlsx：18 笔 / 支出 356.98 / 收入 889.11；支付宝 CSV：44 笔 / 支出 711.64 / 收入 9.72，跳过中性 125 + 关闭 2）。②**收支分类扩充**：支出新增 美容美发 beauty / 数码家电 digital / 运动户外 sport / 网络虚拟 virtual / 房屋住宿 housing / 转账支出 transfer（原 12 类→18 类）；收入新增 退款 refund / 理财收益 invest / 转账收入 transfer（原 6 类→9 类），与微信支付宝交易分类全覆盖对应。③**导入面板隐私说明**：注明本站为纯静态网页、账单仅在本机浏览器解析、不上传任何服务器、记录只存 localStorage、清除浏览器数据会一并清空。④**生活费模拟重构三合一**（`BudgetSim.vue`）：估算（按习惯 / 按家庭收入两模式）/ **预算分配器**（输入月预算 + 拖动 10 类占比滑块实时算金额与结余、一键套用极简省/标准/舒适档位、保存方案到 localStorage `qdu_budget_plan`、下次打开恢复）/ **账单校准**（读 localStorage `qdu_budget_records` 自动算最近 3 个有支出月份的均值与构成、与参考区间对比给结论、可一键把真实占比抄进分配器）。⑤**隐藏彩蛋**（`Budget.vue`，触发隐蔽、反馈为轻量 toast/动画、每项用 localStorage `qdu_eggs` 记录 24h 内只触发一次、不影响正常情绪反馈逻辑）：金额彩蛋——生活费一次性 >10000 且当月盈利 >100000「吹牛逼呢😅」、当月亏损 100000~1000000「你给双子楼炸了😰？」、亏损 >1000000「百万负翁，吹牛逼呢😅」、聚餐费 >100「呦，吃了顿漂亮饭😋」、闯祸费分档（≤50 暖壶 / ≤100 碎碎平安 / ≤500 三国杀 / ≤1000 😨 / ≤10000 撞车 / ≤100000 坐牢 / 更大吹牛逼）、其它支出 9876547210.33「你买b站手办了？」、红包奖金 500000「你抓到间谍了🫨？」、金额 404「收支未找到…」；隐藏皮肤「赛博账本」（连续点「收入/支出」5 次或长按 3 秒解锁，localStorage `qdu_cyber`，霓虹渐变 + 等宽发光数字）；像素雨（快速点「生活费模拟」按钮 3 次，90 像素块下落 3 秒）；节日结算按钮变色（春节农历表 ±3 天 / 愚人节 4-1 / 校庆 5-14，金红/彩虹/青大蓝）；成就「奶茶品鉴师」（food+备注含奶茶累计 20 笔，localStorage `qdu_ach_tea`，明细底部常驻徽章）；彩蛋文本（金额 2 + 备注 `1+1=` 连点记入 10 次，连击期吞掉重复保存避免脏数据）。⑥**默哀模式**：纯静态后门 + 自动计时——`App.vue` onMounted fetch `BASE_URL + 'mourning'`，部署时向 `public/` 上传一个 `mourning` 文件即触发 `document.documentElement.classList.add('mourning')`（全站灰白 + 顶部哀思条）；**另每年 12 月 13 日南京大屠杀死难者国家公祭日自动变灰白**（每分钟定时器检查，进入当天即触发、跨日自动恢复），两种来源显示不同哀思条文案；fetch 会校验响应 Content-Type 非 text/html（排除本地 server 的 SPA fallback 把任意路径回退 index.html 导致的误触发，GitHub Pages 缺失文件为 404 不受影响）；CSS 逐区块 `filter: grayscale(1)`（header/main/footer/bottom-nav/welcome），避免在 html 根 filter 导致 fixed 导航定位失效，不影响可读性与交互。⑦新增 `scripts/test_billimport.mjs` 解析器单测（虚构样本、不含真实数据，`node scripts/test_billimport.mjs` 运行，覆盖支付宝/微信/非账单 3 类）；**`example/` 目录加入 `.gitignore`**（含本机账单个人隐私与 base.apk，禁止入库）。验证：Node 解析单测 3/3、Python 单测 18/18、构建通过、真实账单解析与官方统计逐项核对一致。 |
| 2026-08-18 | v1.3.4 | **独立访客统计修正**。修复校园网场景 UV 严重低估（PV 正常累加、UV 几乎不动）：原按「出口公网 IP + UA」指纹去重，校园 NAT 共享出口 IP + 手机 UA 雷同导致大量真实访客被算成同一人。改为**前端匿名访客 ID（vid）优先去重、缺失回退「IP+UA」指纹**：新增 `src/utils/visitor.js`（`visitorId()`，localStorage `qdu_nav_vid`，`crypto.randomUUID()` 生成，回退时间戳+随机；localStorage 不可用返回空串）；`VisitStats.vue` 与 `router.js` 的 `reportApp` 上报均带 `?vid=`；`counter/server.ts`（生产 Deno）与 `counter/server.mjs`（Node）指纹逻辑同步为 `fingerprint = vid || (ip+'|'+ua)`（uvKey `['v',fp]` / dayUvKey `['vd',dk,fp]` 不变）。**未清除既有 UV/PV 历史数据**（旧 `['v',hash]` 键保留），仅新访客按 vid 计入；不清 localStorage 的现有访客切换后可能被重新计入一次（口径切换预期）。已知权衡：清浏览器数据/无痕/换设备计为新访客、跨设备同一人多计（与行业 cookie 统计一致）；vid 纯匿名随机串不含个人信息。验证：Node 版 counter 模拟（不同 vid 各 +1 / 同 vid 不涨 / 无 vid 走 IP+UA 兜底去重）、构建通过、回归 v12–v19 共 125 项 + 深色 12 项 + 校领导端到端 10 项全绿。另：`counter/server.ts` 与 `server.mjs` 中保留的 seed 校准（150/260）是**早期误删本地数据后手动恢复的痕迹**——只补顶层 uv/pv、未同步 byDay/byRef/byDevice 等维度明细，故**累计 UV/PV 会大于各维度加总**（已知口径差异，代码注释已如实说明，做占比/趋势分析时留意）。 |
| 2026-08-18 | v1.3.3 | **代码质量加固版**（全面代码审查整改：前端 78 分 → 补强）。①**P0 真实 Bug**：`server/index.mjs` 通知详情标题 `split('-')[0]` 截断含连字符标题（「2026-2027学年…」→「2026」）→ 改剥离尾部「-教务处/青岛大学」站名后缀；v-html XSS 过滤补强——`on\w+` 事件属性兼容双引号/单引号/**无引号**/**HTML 实体(&quot;)** 四种写法（此前可被 `<img onerror=alert(1)>` 绕过）；②**死代码清理**：`Timetable.vue` 死 ref `meta`、`StudentId.vue` 死字段 `form.name`、`scripts/snapshot.mjs` 死函数 `parseHomeNotices`、`validate.py` 未使用 `TERM_RE`；③**validate.py 加固**：TERM_RE 接上学期格式校验 + `int()` 异常保护 + `tables[0]` 空数组防护；④`parse_kcb.py` 移除硬编码开发机路径（缺参报错退出）；⑤**P1 一致性**：server 网关 `clsSplit` 与前端 `utils/course.js` 对齐（展开 `[01-04]班` 范围，网关/静态搜索不再结果不一致）；`BuildingMatch.vue` 手机端挑战难度降列（@media ≤640px → 3 列）；`api/index.js` 文件头注释更正（`/notice` 纯静态下如实降级）；`Canteen.vue` 营业时间魔法数字加分钟/小时档注释；⑥**P2 可维护性**：新增公共组件 **`BarRow` / `KpiCard` / `InsightPanel`**（`src/components/`），`CourseStats`（95 处内联样式→约 1/5，空 style 清理）/ `SiteStats` / `TiebaSentiment` / `Budget` 复用；`scripts/tieba.mjs` **重写对齐 Python 手机版方案**（mo/q/threadlist + 移动 UA 池 + PAGE_SIZE 30 + tl_shadow 结构 + normDate 含 HH:MM，Node 回退不再走易 403 的桌面版）；`utcnow()` 三处重复抽 `crawler/util.py`；counter 校准标记独立 `.seed3` 文件（不再泄漏 seed3 字段到 data.json）+ UV 去重 Set→Map 定期清理防无限增长；server cache 超 200 条清理过期项、`/api/notices?all` 的 `cached` 语义修正（此前恒 false）、HTTP 方法限制（非 GET → 405）、MIME 补 `.txt`；`FoodWheel` 动画时长抽 `SPIN_MS` 常量（与 CSS transition 注释同步）、`QuizGame` 每题分数抽 `PER_QUESTION`、`Budget` 记录 id 抽 `newId()`；`LeaderTest` 悬空双注释修正。验证：Python 单测 18/18（含 tieba.mjs 手机版解析与 normDate 单独验证）、构建通过、回归 v12–v19 共 125 项全绿 + 深色专项 12 项 + 校领导端到端 10 项。 |
| 2026-08-18 | v1.3.2 | **深色模式修复 + 维护整理**。①**深色全面修复**：顶栏 `.header`/底部导航 `.bottom-nav` 写死白色改 `--header-bg`/`--nav-bg`；**「关于本站」卡片修复 `--card-bg` 未定义变量**（→ `var(--card)`，此前深色下仍白底）；`.tab`/`.chip`/`.seg-btn`/`.year-tab`/`.mini-tile`/`.score-bar`/`.news-thumb`/骨架屏/悬停高亮/通知提示条/欢迎页 motto 等全部改为语义化 CSS 变量（`--soft-gray`/`--soft-fg`/`--hover-bg`/`--sk-a/b`/`--notice-*`/`--soft-green-*`/`--soft-red-*` 等，`[data-theme=dark]` 逐一定义）；`App.vue` 同步浏览器 `theme-color` meta；②**Bug 修复**：`Canteen.vue` 三处无效 CSS `var(--card)3e0`→`var(--soft-yellow)`；8 处 `var(--muted)` 未定义→`var(--text-sub)`；`termTimetable.js` Web Worker 并发请求消息错配（加自增 id 匹配）；生活费计数器编辑状态下回车误调 `add()` 新增而非 `save()` 保存（统一走 `save()`，删死代码 `add()`）；`LeaderTest.vue` `whyText` 用户向量（-1..1）与原型 vec（0..10）标尺不一致（统一经 `norm()` 映射到 0-10）；③**维护整理**：删除根目录冗余 `leader/` 文件夹（正源 `public/leader/`，.gitignore 移除 `/leader/`）；校领导照片路径改拼 `import.meta.env.BASE_URL` 兼容子路径部署；删除无引用的死数据 `src/data/timetable.js` 与死函数 `CampusNews.open`/`LeaderTest.dimAlign`；`tieba.py` 的 `analyze()` 支持注入 `today`（单测不再随日期失效）；清理计数上报已弃用的 `isNewUv` 参数（UV 去重由服务端 IP+UA 指纹完成，更新 server.ts/mjs 注释）；题数文档勘误（校领导测试实际 **35 题** = 参考 17 + 青大 18，原注释/README 写 30 题）；`package.json` 版本对齐 site.js；④新增回归基线 `regress_dark.mjs`（深色专项 12 项）与 `regress_leader.mjs`（校领导 35 题端到端 10 项）。验证：Python 单测 18/18、构建通过、回归 v12–v19 共 125 项全绿 + 深色专项 12 项 + 校领导端到端 10 项。 |
| 2026-08-17 | v1.3.1 | **校领导测试**。参考 leadertest.site 多维原型比对机制改造为青大版：`src/data/leaders.js`（DIMS 9 维；leaders **10 原型**：竺苗龙/徐建培/夏临华/王安民/范跃进/夏东伟/胡金焱/魏志强/孔伟金/李建波，各含 period/role/bio/summary/vec(0-10)/weight/photo，**真实证件照** public/leader/ 下 10 张 png；questions **30 题**：参考站原题 17 道（含 3 象限题映射 axis、multi 主/次权重、likert、policy）+ 青大特色 13 道）+ `src/views/LeaderTest.vue`（支持 binary/single/multi/likert/policy 题型与每题权重；匹配改为**用户向量归一化到 0-10 后加权距离**，结果多样；结果页含照片/契合度%/**多维比对逐维「你 X / 原型 Y」双条**/契合分析/简要事迹/最接近两位/分享/反馈/再测；`server/index.mjs` serveStatic 增加 decodeURIComponent 修复中文照片文件名 404）；注册 apps.js（游戏组）与 router.js。验证：构建通过、回归 v12–v19 全绿（v19 版本断言放宽 v1.x.y）、375px 手机视口实测（两组极端答案出不同人徐建培/夏临华、照片加载成功、维度数值对照显示）。 |
| 2026-08-17 | v1.3.0 | **舆情统计**。①`counter/server.ts` + `server.mjs` 升级**多维自动采集**：KV `['stats']` 单 key 存 `byDay/byHour/byWeekday/byDevice/byOs/byRef/byApp`，`/api/hit` 按 isNewUv 记 UV/PV 并自动累计日期/小时/星期/设备（UA 解析手机/平板/桌面）/系统（iOS/Android/Windows/macOS/Linux）/来源（Referer 解析直接/GitHub/搜索/社交/外链）/应用；`/api/stats` 返回近 7 天 + 今日 + 24h + 星期 + 设备/系统/来源 + 热门应用；②新增应用 `src/views/SiteStats.vue` **本站舆情**（apps.js/router.js 注册）：KPI 四卡（独立访客/累计访问/今日访客/今日访问）+ 近 7 天柱状图 + 24h 时段 + 一周分布 + 设备/系统/来源条形 + 热门应用 Top（appId→标题映射）；③**纯自动化上报**：`router.js` openApp 调 `reportApp(id)`（同 app 5s 节流，isNewUv=0 不重复 UV）；④`VisitStats.vue` 布局：note 移到数字下方单独一行（手机端不再并列）。验证：构建通过、回归 v12–v19 共 125 项全绿、本地 Node counter 多维 hit 实测（hour=22/devices=手机/os=iOS/refs=直接/apps=timetable 均记录）、375px 手机视口实测（note 在下、本站舆情图表齐全、首页网格含本站舆情）。 |
| 2026-08-17 | v1.2.10 | **功能增强**。①`Budget.vue`：明细记录支持**编辑/删除**（✎ 载入表单，`save()` 原地更新 + `cancelEdit`；删除按钮 title=删除）；**生活费固定收入视角**（`allowance`/`budgetBalance`/`allowanceUsed` computed + 进度条 + `balanceMsg` 按生活费逻辑提示）；新增 `BudgetSim.vue` **生活费模拟**（入口按钮在计数器顶部）：简单版按伙食/社交/购物三档推荐区间、精细版按家庭月收入分档推荐 + 预算明细 + 省钱/兼职建议；②`styles.css` **Logo 炫彩渐变**：`.brand-logo` 白底卡片 + 文字彩虹渐变（`background-clip:text; color:transparent`）；③`site.js` wiki 文案实事求是（校园生活手册）；④`Contributors.vue` `.cloud-role` 防溢出（ellipsis/max-width）+ 社区贡献记录补 v1.2.1–v1.2.10；⑤`Timetable.vue` 手机紧凑网格（`@media ≤640px` 隐藏节次列、7 列均分一屏、行高 30px、卡片仅课程名 3 行截断）。验证：构建通过、回归 v12–v19 共 125 项全绿（v15 删除断言改 title=删除、v16 手机 grid 高改 30px*12）、375px 手机视口实测（logo 渐变 / budget 编辑与生活费 / 模拟页 / 贡献者无溢出 / 课表一屏）。 |
| 2026-08-17 | v1.2.9 | **统计功能**。新增 `src/components/VisitStats.vue`：首页「关于本站」面板内、贡献者墙按钮下方展示**独立访客 UV / 累计访问 PV**，数据来自**自建独立计数服务**——`counter/server.ts`（Deno Deploy 新平台 console.deno.com，`Deno.serve` + `Deno.openKv` KV 持久化，生产地址 `https://qdu-nav.iceoftea.deno.net`）与 `counter/server.mjs`（Node 版，落盘 `counter/data.json`，已在 .gitignore）；UV 去重由前端 localStorage + `isNewUv` 参数完成（跨域第三方 Cookie 可能被拦截）。部署要点：console.deno.com 新建 app（App Directory 选仓库根勿选 src、Runtime=Dynamic、Entrypoint=`counter/server.ts`、Install 留空、Build 命令 `echo skip` 规避容器类型环境差异、创建并 Attach KV 数据库）；组织未验证不影响使用。与 QDU-Wiki 的 Vercount 统计完全独立（实测 Vercount JS 无 `page_uv` 锚点回填、site_uv 按域名共享，同域两站无法独立，故自建）。验证：构建通过、回归 v12–v19 全绿、生产 `/api/stats` 返回 200 `{"uv":0,"pv":0}`、本地实测（首次 uv+1、二次仅 pv+1，localStorage 去重生效；卡片位于贡献者墙按钮下方）。 |
| 2026-08-17 | v1.2.8 | **课表逻辑优化**。①`src/utils/course.js` 的 `clsSplit` 展开「[01-04]班」范围（正则 `^(.+?)\[(\d{1,2})\s*[-–]\s*(\d{1,2})\](班)?$`，补 2 位零）：班级列表只剩单班（25软件01班…04班），不再与「25软件[01-04]班」并存，合班课自动落到每个单班课表、counts 计数随之正确；②`Timetable.vue` 的 `dayCourses` 按「课程+节次」聚合分段授课（同班同节次多位教师合并为一条，教师名全列，周次并集）；③列表视图新增 `clsNote` 合班备注「📌 合班 25软件[01-04]班…」；④确认「21软工创新班」在 2023–2024 学年学期可正常检索（数据在 2024 春/2024 秋等学期，此前为学期/时序误判）。验证：构建通过、回归 v12–v19 全绿、375px 手机视口实测（25软件 无范围条目、读写译 12 师合并 1 条、合班备注正常、搜「21软工」命中 21软工创新班）。 |
| 2026-08-17 | v1.2.7 | **课表体验**。①`Timetable.vue` 的 `subOf` 班级模式改为「教师 · 教室」（数据来自课程总表 `t` 列，此前班级课表只显示教室）；②新增**列表视图**（`viewMode='list'`）：按星期分组、每门课一行（节次 / 课程 / 教师 / 教室），手机端不再挤在窄网格（375px 下网格每列仅约 48px）；③**点击课程弹详情**：`detail` ref + overlay，展示 教师 / 教室 / 班级 / 时间 / 周次 / 校区 / 类别 / 学分；④周视图 / 列表视图一键切换。验证：构建通过、回归 v12–v19 全绿、375px 手机视口实测（网格 53 卡显示教师名、列表 53 项、详情弹窗字段齐全）。 |
| 2026-08-17 | v1.2.6 | **性能优化**。①`Timetable.vue` 的 `termRows` 改 `shallowRef`：避免 Vue 深度代理 5900+ 元素数组（每次属性访问走 Proxy 拖慢遍历）；②`curRows` 直接返回当前学期数据（onMounted/switchTerm 已按学期载入），省重复 filter；③`termTimetable.js` 新增**内联 Web Worker** 后台 `JSON.parse`（2MB 排课数据解析移出主线程），低端机/弱网加载时主线程可正常点击返回/导航。验证：构建通过、回归 v12–v19 全绿、CPU 4x 模拟手机实测冷启动约 0.6s / 搜索 0.3s / 教师与教室 tab 各 22 项正常。 |
| 2026-08-17 | v1.2.5 | **性能优化**。①**课程表卡顿彻底修复**：`Timetable.vue` 的 `countOf(name)` 对每个班级全量 `filter` 遍历排课（O(n²)，约 1470 班级 × 5900 行 ≈ 870 万次，搜索/筛选时主线程卡死、手机无法退出）改为**一次性预计算 `counts` Map**（`computed` 依赖 curRows+tab，一次遍历建 Map，`resultItems` O(1) 查询）；实测打开 **8548ms → 277ms**（快约 30 倍）、搜索响应 264ms；②**智能分页**：`v-for="p in pageCount"` 罗列全部页码（147 个按钮，手机 DOM 巨大且难看）改为 `pageNos` 折叠（`« ‹ 1 2 … 末几页 › »` + `jumpPage` 输入数字跳转 + 首页/末页），expandAll 时隐藏页码。验证：构建通过、回归 v12–v19 全绿、375px 手机视口实测（加载 277ms、搜索 264ms、分页 10 按钮 + 省略号）。 |
| 2026-08-17 | v1.2.4 | **体验优化**。①**课程表加载提速（根因修复）**：`localCourse.js` 的 `staticCourseTable` 改用 2KB `timetable_meta.json`（原回退 `loadSnap()` 全量 15MB snapshot 是卡顿真凶）；`Timetable.vue` 移除 `apiFetch('/courseTable')` 阻塞（本地网关不可达时触发 10s 超时 + 15MB fallback）；实测打开约 217ms；②**官方新媒体链接修正**：微博 `https://www.weibo.com/u/5726029829`、抖音 `douyin.com/user/MS4wLjAB…`、公众号 `mp.weixin.qq.com/s/o6vjr3toGHgJjSUABA26_A`，并新增 **B 站** `space.bilibili.com/441692154`；③**邮箱助手美化**：卡片式渐变 + emoji 图标头 + 俏皮话「校长邮箱 xiaozhang@qdu.edu.cn」。验证：构建通过、回归 v12–v19 全绿、课程表加载实测 217ms。 |
| 2026-08-17 | v1.2.3 | **体验优化**。①**课程表加载提速**：新增 `crawler/split_snapshot.py` 按学期拆分排课数据（`public/data/timetable_meta.json` + `terms/t*.json`，当前学期约 2MB），前端 `src/api/termTimetable.js` 默认只加载当前学期、切换学期懒加载并缓存（替代一次性 fetch+parse 15MB 全量快照），解决首屏慢 / 手机卡死；snapshot.yml 接入 split 步骤并提交拆分产物；②**校园邮箱**：官网「信息化与服务」新增 `mail.qdu.edu.cn` 入口，官网页内嵌**邮箱助手**（输入学号一键生成 `学号@qdu.edu.cn` + 复制 + 开通指南）；③**官网改造**：青大 Wiki 收敛为网站入口（`iceoftea.github.io/QDU-Wiki`）、「信息化与服务」组上移至第 2 位并 `featured` 突出「网上办事大厅」、新增「官方新媒体」组（微博 / 微信视频号 / 抖音，账号 qddx 已查证）；④**贴吧趋势交互**：柱子 hover/点击显示当日帖数（`activeTrend` + tip），窄屏 `.trend` 横向滚动；⑤**手机端修复**：体测页 `.split-row` input 溢出（flex:1 min-width:0，scrollWidth 421→375）、首页 wiki 按钮竖排（`.wiki-links` flex-wrap + nowrap）；⑥应用顺序与底部导航 `NAV_APPS` 调整为指定顺序（首页 / 动态 / 官网 / 生活费 / 体测 / 教室 / 校历）。验证：构建通过、Python 单测 18/18、回归 v12–v19 全绿（v15 顺序断言适配、v18 新增官网/邮箱断言、手机视口 375px 溢出检测通过）。 |
| 2026-08-17 | v1.2.2 | **界面增强**。①课程表班级列表**每页 10 条**（原 50 条，短列表更清爽），页码选页 + 展开全部/收起不变；②**贴吧舆情界面图表化丰富**（参考数据洞察）：新增 KPI 概览卡（抓取帖数 / 最热帖回复 / 话题覆盖 / 近 14 天发帖与日均）、自动文字洞察、「热帖榜」改回复数条形图 + 作者/日期副标题、关键词改**标签云**（字号随热度放大）、话题分布显示占比百分比、趋势柱状图标注日均。验证：构建通过、回归 v12–v19 共 119 项全绿（v18 新增 KPI/洞察/标签云断言，v19 分页断言 50→10，v17 分页断言适配）。 |
| 2026-08-17 | v1.2.1 | **维护增强**。①修复 **GitHub Actions 部署失败**：`tests/test_tieba.py` 解析样本仍为桌面版 HTML，与贴吧手机版 `mo/q/threadlist` 解析不一致 → `parse_threads` 返回 0 导致 unittest 失败、deploy 的 `python3 -m unittest discover` 步骤中断；已把样本更新为手机版结构，单测恢复 18/18；②**数据洞察附带列数据恢复**：本机重新抓取 jwc 课程总表生成快照，`snapshot.json` rows 恢复 `col/campus/kind/cat/credit/weeks` 附带列，`course_stats.json` 的 kindDist（课程 50014/实践 407/美育 194）/ campusDist（浮山 33604/金家岭 16234/松山 790）/ colDist 恢复真实分布（未标注仅剩 2 条）；③数据洞察**界面增强**：新增 KPI 概览卡（最忙校区 / 最忙星期 / 开课学院）、自动生成文字洞察、「每天节次分布」面板（1-2 至 9-10 节）；④课程表**周课表单元格显示校区**。验证：Python 单测 18/18、构建通过、回归 v12–v19 共 115 项全绿（v17 分布断言更新为「显示真实分布 + KPI + 节次分布」）。 |
| 2026-08-17 | v1.2.0 | **功能迭代**。①新增**贡献者墙**：`Contributors.vue` 词云式展示项目贡献者（IceofTea / Huanuyn / qdu-nav-bot），点击跳转 GitHub 主页，头像直链 `avatars.githubusercontent.com`（无需 API key，加载失败自动回退 emoji 徽章），首页「关于本站」面板与首页网格双入口；②**贴吧舆情**：改用百度贴吧**手机版 `mo/q/threadlist` 接口**（iPhone/Android 移动 UA，规避桌面版 WAF 403），真实抓取青岛大学吧帖子产出热帖榜 / 关键词 / 话题分布 / 14 天趋势（本机实测成功 119 帖，GitHub Actions 定时任务同步生效）；抓取失败时前端显示如实空态而非报错，并保留 status:empty 空结构防 HTTP 404；③课程表班级列表**分页**：默认每页 50 条 + 页码选页 + 「展开全部/收起」，搜索与筛选自动回到第 1 页，解决 1470+ 班级全量渲染卡顿；④数据洞察**分布科学化**：课程性质 / 校区 / 学院分布仅在存在真实标注数据时展示，过滤「未标注」项，占比按「已标注样本数」计算（未标注拉低占比的问题消除），旧快照无附带列时显示提示面板并隐藏空分布；⑤**奖学金金额校准**（Budget.vue）：优秀奖学金 1500/1000/500→**1000/600/400**、国家助学金 4400/3300→**2300/3300/4300**（青大学字〔2021〕27号），并增补竞赛奖学金（挑战杯·国特 30000 / A类·国一 5000）与博学奖学金（发明专利 5000）预设，附来源说明。验证：Python 单测 18/18、构建通过、回归 v12–v19 全绿（v18 版本断言放宽为 v1\.\d+）；另修复 PR #1 网页合并时被冲掉的两处实现（server `PY` 平台适配定义、快照附带列），本地三文件与自研实现逐字节一致。 |
| 2026-08-17 | v1.1.1 | **社区修复版**（合入贡献 PR #1，致谢 huanuyn）。①`server/index.mjs` 课程接口报错：`PARSE_PY` 原本指向 `os.tmpdir()/qdu_parse_kcb.py`（不存在），改为 `server/parse_kcb.py`，并按平台选择 `python`（win32）/`python3`（类 Unix）；②`FoodWheel.vue` 美食轮盘第二次起指针停靠角与抽取结果不一致：将当前旋转角归一化后补足到目标扇区中心，而非把绝对目标角直接累加；③PR 还独立修复了「快照保留 kind/campus」——与 v1.1.0 已有实现重复且实现较简（未含 col/cat/credit/weeks 与 colDist），**未重复合入**。验证：Python 单测 18/18、构建通过、回归 v12–v18 全绿。 |
| 2026-08-17 | v1.1.0（2 次提交） | **功能迭代**。①数据链路修复：快照保留 `col/campus/kind/credit/weeks` 附带列，`analysis.py` 新增 `colDist`，数据洞察新增课程性质 / 校区 / 学院开课分布与解读（旧快照为「未标注」，下次定时抓取自动补齐）；②课程表班级列表全量展示（移除 40 条截断）；③生活费计数器增强：类别扩充 + 微信/支付宝账单 CSV 导入自动归类 + 奖学金/助学金预设一键带出 + 结余情绪分档与月对比；④青大知多少题库扩至 102 题 + 选项徽章 + 成绩页错题回顾与本机排行榜；⑤今天吃什么支持校区筛选；⑥**新增贴吧舆情**：`crawler/tieba.py` + `scripts/tieba.mjs` 尽力抓取「青岛大学吧」公开列表页，产出 `tieba_stats.json`（热帖榜 / 关键词 / 话题分布 / 近 14 天趋势），前端 `TiebaSentiment.vue`，接入 snapshot.yml（失败不阻塞，旧数据保留），删除原「评论区」本地演示应用（纯本地无社交意义）；⑦学校官网：学院官网按「人文社科 / 理工 / 医学 / 艺术与体育 / 合作办学」分类展示，社区与资源组新增青岛大学吧链接。验证：Python 单测 18/18，构建通过，回归 88/88 + 新增 v18（贴吧舆情 / 学院分类 / 贴吧入口）。 |
| 2026-08-17 | v1.1.0（1st） | 本日志之前记录于会话内。含：数据链路修复、课程表全量、生活费增强、题库 102 题、今天吃什么校区筛选。（见 README 版本历史 v1.1.0） |
| 2026-08-16 | v1.0.0 | 前端工程化重构为可移植模板（config/site.js、router.js、utils/），版本号体系建立。详见 README 版本历史。 |
